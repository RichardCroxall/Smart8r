// smart8r.cpp : Defines the entry point for the console application.
//

#include "include/runtime.h"

/*

int main(int argc, char* argv[])
{
    for (int arg = 1; arg < argc; arg++)
    {
        if (strcmp(argv[arg], "batch") == 0)
        {
            interactive = false;
        }
        else
        {
            fprintf(stderr, "arg[%d]=%s not understood\n", arg, argv[arg]);
        }
    }

#if DEBUG22
    logging.logInfo("sizeof short %d\n", sizeof(short));
    logging.logInfo("sizeof int %d\n", sizeof(int));
    logging.logInfo("sizeof long %d\n", sizeof(long));
    logging.logInfo("sizeof long long %d\n", sizeof(long long));
#endif

    printf("Hello world\n");

    readFile();
    runtime();
    return 0;
}

*/